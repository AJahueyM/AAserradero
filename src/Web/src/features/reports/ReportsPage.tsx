import { FeaturePlaceholder } from '../FeaturePlaceholder';

// Replaced by the reports feature agent (export + occupancy reports).
export default function ReportsPage() {
  return <FeaturePlaceholder labelKey="nav.reports" descriptionKey="routes.reportsDescription" />;
}
