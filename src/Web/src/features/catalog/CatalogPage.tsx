import { FeaturePlaceholder } from '../FeaturePlaceholder';

// Replaced by the catalog feature agent (areas / rooms / concepts admin).
export default function CatalogPage() {
  return <FeaturePlaceholder labelKey="nav.catalog" descriptionKey="routes.catalogDescription" />;
}
